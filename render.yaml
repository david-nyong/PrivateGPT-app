services:
  - type: web
    name: my-service
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app -k uvicorn.workers.UvicornWorker --timeout 1500 && streamlit run streamlit_app.py --server.address localhost"
